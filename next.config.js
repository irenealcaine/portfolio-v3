/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
  i18n: {
    locales: ['en', 'es'], // Agrega los idiomas que necesitas
    defaultLocale: 'es', // Establece el idioma predeterminado
  },
};
